import { PreventDirective } from './prevent.directive';

describe('PreventDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventDirective();
    expect(directive).toBeTruthy();
  });
});
